name = "elira-checkout"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets (set via CLI, never commit real keys):
#   wrangler secret put STRIPE_SECRET_KEY
#   wrangler secret put ALLOW_ORIGIN     # e.g. https://<user>.github.io
